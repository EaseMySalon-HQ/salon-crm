/**
 * Placeholder for digest / batched reminder rules (see Booking.metadata.reminderPolicy).
 * Wire to cron + existing notification senders when product is ready.
 */
function buildReminderBatchForClient() {
  return [];
}

module.exports = {
  buildReminderBatchForClient
};
