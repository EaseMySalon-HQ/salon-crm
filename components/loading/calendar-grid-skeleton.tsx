import { PageSkeleton } from "@/components/loading"

/** Calendar grid loading placeholder matching staff-column layout. */
export function CalendarGridSkeleton() {
  return <PageSkeleton variant="calendar" />
}
